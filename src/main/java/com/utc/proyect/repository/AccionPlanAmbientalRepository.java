package com.utc.proyect.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.utc.proyect.entity.AccionPlanAmbiental;

public interface AccionPlanAmbientalRepository extends JpaRepository<AccionPlanAmbiental, Long> {
}

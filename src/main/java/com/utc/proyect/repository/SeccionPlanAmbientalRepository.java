package com.utc.proyect.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.utc.proyect.entity.SeccionPlanAmbiental;

public interface SeccionPlanAmbientalRepository extends JpaRepository<SeccionPlanAmbiental, Long> {
}
